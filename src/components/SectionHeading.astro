---
interface Props {
  eyebrow: string;
  title: string;
  intro?: string;
}
const { eyebrow, title, intro } = Astro.props;
---

<div class="section-heading" data-reveal>
  <p class="eyebrow">{eyebrow}</p>
  <div>
    <h2>{title}</h2>
    {intro && <p>{intro}</p>}
  </div>
</div>
