# Yashaswi Makula — Portfolio

A static, performance-first personal portfolio built with Astro and deployed through Cloudflare Workers Static Assets.

## Local development

```sh
npm install
npm run dev
```

## Production

```sh
npm run build
npm run deploy
```

Set `SITE_URL` during the build when deploying to a custom domain. The generated site is written to `dist/`.
